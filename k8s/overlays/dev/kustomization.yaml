apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

nameSuffix: -dev

patches:
  - target:
      kind: Ingress
      name: moogle-ingress
    patch: |-
      - op: replace
        path: /spec/rules/0/host
        value: dev.moogle.local
